# Kind cluster for agent-safe integration tests.
# Usage: kind create cluster --config infra/kind-config.yaml --name agent-safe-test
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
